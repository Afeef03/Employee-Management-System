import { Toolbar } from "@mui/material";
import React from "react";

const Logo = () => {
  return (
    <Toolbar>
      <h2 className="text-bold text-xl text-primary font-family font-bold">
        EMS By <span className="font-normal">Afeef</span>
      </h2>
    </Toolbar>
  );
};

export default Logo;
